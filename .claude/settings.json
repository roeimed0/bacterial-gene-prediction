{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); echo \"$f\" | grep -q '\\.py$' && black --line-length=100 \"$f\" 2>/dev/null || true",
            "statusMessage": "black formatting..."
          }
        ]
      }
    ]
  }
}
