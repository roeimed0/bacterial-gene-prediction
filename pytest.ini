[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (run with -m slow, excluded from fast tier)
addopts = -v --tb=short
