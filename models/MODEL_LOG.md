# Model Log

Records trained artifact provenance for models in this directory.

---

## orf_classifier_lgb.pkl  ← **CURRENT PRODUCTION** (promoted 2026-05-31, v3)

| Field | Value |
|---|---|
| Type | LightGBM binary classifier (OrfGroupClassifier) |
| Task | Predict whether a group of nested ORFs contains a real gene |
| Features | **30 named group-level features** (see feature_names.pkl) |
| Training data | 68 genomes from GENOME_CATALOG (stratified by taxonomy, seed=17) |
| Validation | 16 genomes (early stopping) |
| Test | 16 held-out genomes |
| Threshold | **0.07** (swept t=[0.05,0.07,0.10] on 20-genome holdout; 0.07 gave best F1) |
| Trained | 2026-05-31 |
| Performance | **F1=74.50%** corrected (pseudogenes excluded from ref; old 73.02% counted pseudogenes as missed) |
| Script | `scripts/training/train_lgb.py --seed 17` |
| Previous | `orf_classifier_lgb_v2_backup.pkl` (34 Column_N features, 2026-05-31) |

**v3 changes vs v2 (ML2 + rbs_dominance):**
- Removed 5 broken `rel_*_max` features — `max(x/max(x)) = 1.0` always (mathematical identity, zero information)
- Added `rbs_dominance = rbs_max / rbs_mean`: dominance ratio, AUC=0.874 vs rbs_max alone at 0.821 (+0.053). Measures how much the best ORF's RBS outperforms the group average.
- Added `genome_gc_high = max(0, genome_gc - 0.55)`: gated GC% signal (zero for low/moderate-GC genomes, positive for high-GC). Diagnostic: r(genome_gc, F1)=-0.677, max corr with existing=0.21.
- Model now uses named features (not generic Column_N) — fixes feature-order safety.
- Systematic gating analysis across all 21 remaining candidates confirmed no further features benefit from gating.

**De novo constraint:** genome_gc is computed from the input sequence alone — no external databases.

---

## orf_classifier_lgb.pkl  ← ARCHIVED BELOW (was v1)

| Field | Value |
|---|---|
| Type | LightGBM binary classifier (OrfGroupClassifier) |
| Task | Predict whether a group of nested ORFs contains a real gene |
| Features | 31 group-level features (see feature_names.pkl) |
| Training data | 68 genomes from GENOME_CATALOG (stratified by taxonomy) |
| Validation | 16 genomes (early stopping) |
| Test | 16 held-out genomes |
| scale_pos_weight | ~1.1 (group-level class ratio is near-balanced post-pipeline) |
| Threshold | 0.07 (calibrated via PR curve on val set) |
| Trained | 2026-05-05 |
| Performance | +2.3pp F1, +3.1pp precision vs prior model on 20 external holdout genomes |
| Script | `scripts/manage_lgb.py train` |

## start_selector.pkl ← **CURRENT PRODUCTION** (2026-05-15)

| Field | Value |
|---|---|
| Type | Pairwise LightGBM classifier (StartSelectionClassifier) |
| Task | Two-stage start codon selection — re-evaluate contested groups (gap < 1.0) using pairwise features |
| Features | 40+ pairwise differences + group-level context (scores, lengths, RBS motifs, anti-SD, ctx PWM, len z-score, post-start codons) |
| Training data | 161K contested pairs from 84 catalog genomes (seed=42 split, leave-one-phylum-out CV) |
| Flip threshold | 0.80 (only flip when classifier is >80% confident) |
| Contest threshold | 1.0 |
| Calibration | LGB (num_leaves=127, min_child_samples=20) + isotonic regression |
| CV accuracy | 88.1% (naive baseline 85.1%) |
| Start accuracy | 86.3% (+2.9pp over 83.4% baseline) |
| Full pipeline F1 | **72.73%** (experiments/log.json entry #4) |
| Trained | 2026-05-15 |
| Script | `scripts/training/train_start_classifier.py --seed 42` |
| Backup | `start_selector_v2_backup.pkl` |

**Ablation study (2026-05-15):** Tested nterm, gc3, groupnorm feature groups individually.
None cleared the 0-regression bar on the 20-genome full pipeline benchmark vs v2.
See experiments/log.json entries #5–#8.

---

## feature_names.pkl

26 feature names for orf_classifier_lgb.pkl, in training order.
Generated by `OrfGroupClassifier.extract_group_features()`.
*(Was 31 features until PRs #123/#125 removed 7 zero-importance features and added 2 new ones.)*

## hybrid_best_model.pkl

| Field | Value |
|---|---|
| Type | CNN+Dense hybrid (HybridGeneFilter) |
| Task | Final binary filter on individual ORF candidates |
| Tabular features | 26 (see HybridGeneFilter.feature_names — updated via PRs #124/#126/#127) |
| Sequence input | One-hot DNA, capped at 1500 bp |
| Architecture | CNNBranch(128) + DenseBranch(26→128) → fusion → 1 |
| Training data | Candidates from LGB-filtered pipeline on 68 GENOME_CATALOG genomes |
| Labeling | Exact coordinate match to reference GFF CDS |
| Loss | BCEWithLogitsLoss(pos_weight=n_neg/n_pos) |
| Threshold | 0.25 (selected by end-to-end F1 sweep on 8 holdout genomes) |
| Trained | 2026-05-06 |
| Performance | +4.24pp mean F1, +3.65pp sensitivity, +2.74pp precision on 20 external holdout genomes |
| Script | `scripts/train_hybrid.py` |

## calibration_report.json

Output of `scripts/calibrate_start_weights.py`.
Confirms START_SELECTION_WEIGHTS are already at local optimum — no changes needed.
